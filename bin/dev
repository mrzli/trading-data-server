#!/usr/bin/env bash
set -euo pipefail

PORT="${PORT:-4567}"

# Kill any existing process on the port using our kill script
"$(dirname "$0")/kill" 2>/dev/null || true

exec bundle exec rackup -s puma -p "$PORT"
